version: '2'

syncs:
    auth-service-sync:
        src: './app/'
        sync_excludes: ['node_modules']
        sync_host_ip: 'auto'
        sync_host_port: 10871
        sync_group: root
        sync_userid: 0
        sync_strategy: 'rsync'
